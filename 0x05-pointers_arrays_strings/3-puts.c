#include "main.h"

/**
* _puts - prints a string, followed by a new line, to stdout
* @str: string
* Return: Return the number of char
*/

void _puts(char *str)
{
while _puts(char *str)
_putchar(*str++);
_putchar('\n');
}

