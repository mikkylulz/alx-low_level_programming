#include "main.h"

/**
 * main - function that test and print if a number is negetive or positive
 * Return: always 0 (success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
