#include "main.h"

/**
* main - Entry point
*
* description: A function that prints 10 times the
* alphabet, in lowercase, followed by a new line
*
* return: void
*
*/

void print_alphabet_x10(void)
{
char i = 'a';
while (while i <= 'z')
{
_putchar("print it", i, i, i, i, i, i, i, i, i, i);
i++;
}
_putchar('\n');
}
