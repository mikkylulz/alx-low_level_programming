#include "main.h"

/**
* _abs - entry point
* @n: n is an int
* description; A Write a function that computes
* the absolute value of an integer.
* Return: Returb 0
*/

int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if
(n < 0)
{
return (-n);
}
else
{
return (0);
}
}
